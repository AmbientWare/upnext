[project]
name = "conduit-workspace"
version = "0.1.0"
description = "Conduit - Modal-inspired task queue and API framework"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
# Workspace root is not a package itself
package = false

[tool.uv.workspace]
members = [
    "packages/conduit",
    "packages/server",
    "packages/shared",
]
